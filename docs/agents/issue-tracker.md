# Issue tracker

Work for this repo is tracked as **local markdown files** under `.scratch/<feature>/`.

- No GitHub/GitLab remote (origin is `git@codeberg.org:yjdyamv/rar-rs`), so tracker CLIs do not apply.
- Each ticket is a markdown file; `triage`, `to-tickets` and `to-spec` read from and write to `.scratch/`.
- PRs as a request surface: off.
